# Install rip grep for serching through the files

$ winget install BurntSushi.ripgrep.MSVC # Windows
$ sudo apt-get install ripgrep # Linux

# Must have a MinGW 64binj
# Installed WinGup something for make

# Installed npm tree-sitter
npm install tree-sitter

# For installing FZF for telescope and need Cmake
winget install fzf
winget install ajeetdsouza.zoxide

# Must install x server on windows X

# TODO
SPACE + ca To recomend code actions
SPACE + rn TO rename multiple options in my code
